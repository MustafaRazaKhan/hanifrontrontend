import React from "react";

const Heading = ({ title }) => {
  return <h1 className="text-center uppercase py-4 text-xl font-bold border-b-4 w-[200px] mx-auto my-4 cursive">{title}</h1>;
};

export default Heading;
